# Import the algorithm files to subscribe them into mantid
# This should mean that the module is usable with out any path modifications.
# They won't make it into the simpleapi though
import vesuvio.algorithms.VesuvioPreprocess
import vesuvio.algorithms.VesuvioTOFFit